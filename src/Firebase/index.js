import FirebaseContext, { useFirebaseContext } from './FirebaseContext';
import withFirebase from './withFirebase';
import Firebase from './Firebase';

export default Firebase;
export { FirebaseContext, useFirebaseContext, withFirebase };